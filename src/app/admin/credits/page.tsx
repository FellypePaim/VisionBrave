import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminCreditsPage() {
  return (
    <ComingSoon
      title="Créditos"
      phase="Fase 2"
      description="Busca de usuário + modal de ajuste (adicionar / remover / refund) com motivo obrigatório e audit log automático."
    />
  );
}
