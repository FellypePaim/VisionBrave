import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminAuditPage() {
  return (
    <ComingSoon
      title="Auditoria"
      phase="Fase 4"
      description="Trilha completa de ações admin — quem fez o quê, quando, com before/after em JSON. Filtros por ação, admin, entidade."
    />
  );
}
